import 'package:flutter/material.dart';

const kAppTitleTextStyle =
    TextStyle(fontSize: 48, color: Colors.white, fontWeight: FontWeight.bold);

const kAppSubTitleTextStyle = TextStyle(color: Colors.white, fontSize: 20);

const kToDoListTextStyle = TextStyle(fontSize: 20, fontWeight: FontWeight.w500);

const kCardBoxDecoration = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.only(
      topLeft: Radius.circular(24), topRight: Radius.circular(24)),
);
